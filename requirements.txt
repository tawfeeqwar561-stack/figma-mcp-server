mcp[cli]>=1.10.1,<2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
websockets>=13.0
# Explicitly declared even though `mcp` already pulls in pydantic v2
# transitively -- design_plan.py, planner.py, design_tokens.py,
# components.py, and plan_validation.py all import pydantic directly, so
# its version should be pinned here rather than left to whatever an
# unrelated `mcp` version bump happens to drag in.
pydantic>=2.7.0,<3.0.0